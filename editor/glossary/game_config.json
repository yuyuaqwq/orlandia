{
  "fields": {
    "battle_config": {
      "zh": "战斗配置表",
      "note": "39 张表（DOT 定义/元素反应/Boss 倍率/机制白名单/倍率与概率…）。真源 `game/data/battle_config.py`；成员名 = 源常量名原样。已排除：4 张 tuple 键表（JSON 不可逆）+ 3 张已属 `panel_rules` 域的表。",
      "widget": "kv",
      "group": "config"
    },
    "battle_rules": {
      "zh": "战斗规则注入表",
      "note": "4 张自有表：`EFFECT_ACTIONS`（70 个效果名 → 动作列表）/ `MECH_CASH`（9 个爆发机制）/ `BAR_INJECT_FIELDS` / `BAR_STATE_PREFIX`。`EFFECT_RULES`/`PASSIVE_PROC` 在别域；`DOT_*` 5 名是 import 进来的（家在 `battle_config`）。",
      "widget": "kv",
      "group": "config"
    },
    "weekly_quests": {
      "zh": "周常常量",
      "note": "`WEEKLY_PICK`（每周发布条数 = 3）/ `WEEKLY_MIN_LV`（解锁等级 = 50）。真源 `game/data/weekly_quests.py:152/155`。包内消费：`content/flow/weekly_progress.py`。",
      "widget": "kv",
      "group": "config"
    },
    "trial_tower": {
      "zh": "修炼塔常量",
      "note": "`TRIAL_DAILY_LIMIT` = 3（每日通关上限）/ `TRIAL_MIN_LV` = 70（解锁等级）/ `TRIAL_MAX_FLOOR` = 30（层数上限）。真源 `game/data/trial_tower.py:24/27/30`。层表在 `trial_floors` 域。",
      "widget": "kv",
      "group": "config"
    },
    "econ_config": {
      "zh": "经济常量",
      "note": "`ECON_CONFIG`（16 键）：住宿费两档与取整、传送费保底、市场定价上下限、商店装备价系数、回收半价、图纸价、购买上限。真源 `game/data/econ_config.py:8`。",
      "widget": "kv",
      "group": "config"
    },
    "signin_config": {
      "zh": "签到配置",
      "note": "`SIGNIN_CONFIG`（6 键）：基础金币/连签每级 +5/节日倍率/运势两阈值/每 7 天额外奖励品质权重。真源 `game/data/signin_config.py:7`。",
      "widget": "kv",
      "group": "config"
    },
    "enhance": {
      "zh": "强化流程常量",
      "note": "`MAX_ENHANCE` = 9（+9 上限）/ `ENHANCE_FAIL_DROP` = {5:1, 6:1}（失败掉级，**int 键**）/ `ENHANCE_SMITH_MAPS`（6 张可强化铁匠铺地图）。表本体 `ENHANCE_TABLE` 在 `enhance_table` 域。",
      "widget": "kv",
      "group": "config"
    },
    "upgrade": {
      "zh": "装备升级",
      "note": "`UPGRADE_TABLE`（1..10 级金币阶梯，**int 键**，用 `content/config.py:int_keys()` 还原）/ `MAX_UPGRADE`（存量兼容）/ `UPGRADE_STONE`（精炼强化石 id）/ `UPGRADE_STAMINA` = 10 / `UPGRADE_MATERIAL_CN`（显示名）。真源 `game/data/upgrade.py`。",
      "widget": "kv",
      "group": "config"
    },
    "calamity": {
      "zh": "怪异炼成",
      "note": "`CALAMITY_MAX` = 3（每件次数）/ `CALAMITY_COST` / `CALAMITY_STATS`（属性池）/ `CALAMITY_BONUS` = 0.03 / `CALAMITY_MALUS` = 0.01 / `CALAMITY_POSITIVE_CHANCE` = 0.90。真源 `game/data/calamity.py`。",
      "widget": "kv",
      "group": "config"
    },
    "refine": {
      "zh": "装备重锻配方",
      "note": "`REFINE_RECIPES`（3 条）：键 = 旧装备中文名 → `{target, mats, gold, inherit, desc}`（`inherit` = full/half 继承口径）。真源 `game/data/refine.py:15`。",
      "widget": "kv",
      "group": "config"
    },
    "housing": {
      "zh": "房产与房屋",
      "note": "`PROPERTIES`（6 处地契）/ `HOUSE_LEVELS`（1..4 级，**int 键**：仓库/升级材料/回家恢复/铺面位）/ `HOUSE_MAX_LEVEL`（派生 = 4）/ `HOUSE_REFUND`（卖房返还比例，**int 键**）。真源 `game/data/housing.py`。",
      "widget": "kv",
      "group": "config"
    },
    "gather": {
      "zh": "露营点与挖掘点",
      "note": "`CAMP_SPOTS`（9 个露营点：地图 → 点名）/ `MINE_SPOTS`（9 个挖掘点：地图 → `{name, min_lv}`，min_lv = 挖掘副业门槛）。真源 `game/data/gather.py`。",
      "widget": "kv",
      "group": "config"
    },
    "gather_pools": {
      "zh": "采集与挖掘掉落池",
      "note": "`GATHER_MAP_POOLS`（68 图采集池）/ `GATHER_COND_POOLS`（8 条件池）/ `MINING_DEEP_POOLS`（9 深矿池）。⚠ 池条目源侧是 tuple → 落盘 array（按序解包/索引不变）。真源 `game/data/gather_pools.py`。",
      "widget": "kv",
      "group": "config"
    },
    "item_tag_display": {
      "zh": "物品属性显示模板",
      "note": "`ITEM_TAG_DISPLAY`（鱼 / 材料 / `*` 通配）：`{line, max_lines, omit}`，`line` 用 `str.format(**tag)` 填槽。真源 `game/data/item_tag_display.py:25`。",
      "widget": "kv",
      "group": "config"
    },
    "prof_config": {
      "zh": "副业配置",
      "note": "15 张表/常量：导师与等待、每日副业任务与经验、体力与背包过滤、典当率与价格带、附魔槽解锁与符文门槛、采点门槛。⚠ 若干成员是 tuple → array；同文件的 `price_band()`/`gather_map_min_lv()` 是函数，不进域。真源 `game/data/prof_config.py`。",
      "widget": "kv",
      "group": "config"
    },
    "formula_skeleton": {
      "zh": "公式骨架数值",
      "note": "`FORMULA_SKELETON`（9 组参数）：exp 兜底、怪物 exp/gold 斜率与补偿、技能成长默认值、技能点定价、副业经验曲线、装备暴击/项链 mdef 修正、boss 旧减速曲线。**只有数值**，公式结构在宿主引擎。⚠ `boss_atk_legacy.seg` 是 tuple 的 tuple → array。",
      "widget": "kv",
      "group": "config"
    },
    "stat_templates": {
      "zh": "怪物与装备数值模板",
      "note": "13 张表：怪物角色 base/growth、exp/gold 基数、野外档位倍率、普通/精英/Boss/副本 Boss 的 HP·ATK 段乘区、角色修正、装备槽基础与成长。⚠ 段乘区是 tuple 的 tuple → array（按序解包/索引不变）。真源 `game/data/stat_templates.py`。",
      "widget": "kv",
      "group": "config"
    },
    "builds": {
      "zh": "流派方案",
      "note": "`BUILDS`：职业 id → `{流派名: {icon, skills, desc}}`（每职业 2-3 套）。`skills` 里是**中文技能名**（指向 `skills` 域，按名引用）。真源 `game/data/builds.py:8`。",
      "widget": "kv",
      "group": "config"
    },
    "mounts": {
      "zh": "坐骑池与掉落",
      "note": "`MOUNT_POOL`（11 只坐骑）/ `MOUNT_DROP_ELITE` / `MOUNT_DROP_BOSS`（掉落率）。`MOUNT_BY_KEY` 是派生索引，**不进域**。真源 `game/data/mounts.py`。",
      "widget": "kv",
      "group": "config"
    },
    "summons": {
      "zh": "召唤物模板",
      "note": "`SUMMONS`（5 条）：`{name, icon, atk_ratio, hp_ratio, def_ratio, dmg_type, limit, bodyguard, rank, reach}`（比率相对召唤者）。真源 `game/data/summons.py:21`。",
      "widget": "kv",
      "group": "config"
    },
    "rules": {
      "zh": "行为彩蛋规则",
      "note": "`RULES`（20 条）：`{id, trigger, desc, cond, count, action}`；`action.template` 指向宿主文案/效果模板注册表（**模板本体未进包**）。真源 `game/data/rules.py:10`。",
      "widget": "kv",
      "group": "config"
    }
  },
  "groups": [
    {
      "id": "config",
      "label": "配置常量组",
      "icon": "⚙️",
      "fields": [
        "battle_config",
        "battle_rules",
        "weekly_quests",
        "trial_tower",
        "econ_config",
        "signin_config",
        "enhance",
        "upgrade",
        "calamity",
        "refine",
        "housing",
        "gather",
        "gather_pools",
        "item_tag_display",
        "prof_config",
        "formula_skeleton",
        "stat_templates",
        "builds",
        "mounts",
        "summons",
        "rules"
      ]
    }
  ]
}

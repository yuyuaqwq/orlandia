{
  "fields": {
    "drops": {
      "zh": "材料权重表",
      "note": "该地图可采到的材料：`[[材料 id, 相对权重], …]`（源是 tuple 列表，JSON 化后成数组）。权重实测 2~40；`mat_*` 指 items 域。注释里的 v167「锻造基础料」（如亚麻织物/月光棉/符文绸）是低权重加料。"
    },
    "seq": {
      "zh": "源插入序",
      "note": "导出期注入（1 基）：源 GATHER_MAP_POOLS 的插入序。折进 DROP_POOLS 时按该序展开 → 顺序变了同 seed 产出漂移。"
    }
  }
}

{
 "fields": {
  "effect_data": {"zh": "效果参数", "note": "配合 `effect` 的参数字典：键与含义由内容侧的效果模板解释（如 `pct` / `turns` / `key` / `amount`）。表单按**键值行**编辑；值本身是对象/数组时行内退化为结构化子编辑器或 JSON 兜底，键不会因为渲染而丢。", "widget": "kv"}
 }
}

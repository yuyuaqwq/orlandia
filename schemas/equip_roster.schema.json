{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://saintessengine.local/schema/equip_roster.schema.json",
  "title": "装备名册（equip_roster）—— 框架模板，取值语义由内容侧定义",
  "$defs": {
    "equip": {
      "type": "object",
      "title": "装备（EQUIP_ROSTER 一条）",
      "required": [
        "name",
        "slot",
        "quality",
        "lv",
        "series",
        "source"
      ],
      "additionalProperties": true,
      "properties": {
        "affix": {
          "type": "string",
          "minLength": 1,
          "title": "固定词条 id（单值旧形态）",
          "description": "单值的固定词条引用（与 affixes 并存的旧形态）；指向词条域的表键，解析由内容侧负责。"
        },
        "affixes": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "title": "固定词条 id 列表",
          "description": "该装备的固定词条引用表（指向词条域的表键）。内容侧生成装备实例时读它，框架不解析。"
        },
        "desc": {
          "type": "string",
          "title": "描述",
          "description": "展示文案。本域**不强制非空**：允许无描述条目（与物品域不同）。"
        },
        "fixed_affixes": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "title": "系列固定词条 id 列表",
          "description": "由内容侧「装备名 → 固定词条 id 列表」源表在**导出期按 name 连接**而来（源表形态见 $defs/series_fixed_affix）—— 不是源条目自带的字段。值 = 词条 id 列表，指向词条域。"
        },
        "legendary": {
          "type": "string",
          "minLength": 1,
          "title": "传说专属特效 id",
          "description": "指向内容侧「传说专属特效表」的 id（框架无对应域，解析由内容侧负责）。"
        },
        "lv": {
          "type": "integer",
          "minimum": 1,
          "title": "等级",
          "description": "该装备的等级档（整数 ≥ 1）。"
        },
        "name": {
          "type": "string",
          "minLength": 1,
          "title": "名称",
          "description": "装备显示名（内容侧词汇）。它是本表的内容侧查询锚点（跨表按名连接即以它为准）。"
        },
        "quality": {
          "type": "string",
          "minLength": 1,
          "title": "品质",
          "description": "品质档名。**取值是内容侧词汇，框架不枚举**（不同游戏的档数/命名不同，枚举会锁死扩展）。"
        },
        "req": {
          "type": "object",
          "x-widget": "kv",
          "propertyNames": {
            "type": "string",
            "minLength": 1,
            "description": "键 = 属性名（内容侧词汇：框架**故意不写 properties**，写了就会长出一份框架侧属性表）。"
          },
          "additionalProperties": {
            "type": "integer",
            "minimum": 0,
            "description": "值 = 需求值（非负整数）。穿戴/购买时逐项比对，不达标就拦下。"
          },
          "title": "属性需求",
          "description": "{属性名: 需求值}。属性名是内容侧词汇（框架不枚举）；值为非负整数。缺该字段 = 无属性门槛。"
        },
        "series": {
          "type": "string",
          "minLength": 1,
          "title": "系列",
          "description": "归属系列名（内容侧词汇；**显示名**而不是 key）。$defs/series_sets 的映射即以它为输入。"
        },
        "series_set": {
          "type": "string",
          "minLength": 1,
          "title": "系列套装名",
          "description": "由内容侧「系列 → 套装名」源表在**导出期按 series 连接**而来（源表形态见 $defs/series_sets）。没有该映射的系列**不写此字段**（缺省 = 无套装名，不是空串）。"
        },
        "set": {
          "type": "string",
          "minLength": 1,
          "title": "套装名",
          "description": "条目自带的套装名（内容侧词汇）。注意它与 series_set 的来源不同：这是源条目里的字段，series_set 是导出期连接的派生字段。"
        },
        "slot": {
          "type": "string",
          "minLength": 1,
          "title": "部位",
          "description": "装配槽位名。**取值是内容侧词汇，框架不枚举**（框架侧不知道也不该知道有哪些槽位）。"
        },
        "source": {
          "type": "string",
          "minLength": 1,
          "title": "来源",
          "description": "获取途径名（内容侧词汇；展示与统计用）。"
        },
        "special": {
          "type": "string",
          "minLength": 1,
          "title": "专属说明",
          "description": "该装备专属效果的说明文本（内容侧词汇，展示用）。与 legendary / weapon_effect 的机器可读 id 并列存在。"
        },
        "we_data": {
          "type": "object",
          "additionalProperties": true,
          "x-widget": "kv",
          "title": "武器特效参数",
          "description": "配合 weapon_effect 的自由参数字典（结构由内容侧定）。表单对自由结构走 JSON 兜底，不会因渲染丢键。"
        },
        "weapon_effect": {
          "type": "string",
          "minLength": 1,
          "title": "武器特效 id",
          "description": "指向内容侧武器特效实现的 id（框架不解析、不校验它是否存在）。"
        },
        "weapon_type": {
          "type": "string",
          "minLength": 1,
          "title": "武器类型",
          "description": "武器细分类型名（内容侧词汇，框架不枚举）。只在武器部位条目上出现；非武器条目**不写此字段**。"
        }
      },
      "examples": [
        {
          "name": "示例短剑",
          "slot": "weapon",
          "weapon_type": "sword",
          "quality": "blue",
          "lv": 12,
          "series": "示例系列",
          "req": {
            "str": 10
          },
          "source": "示例来源",
          "desc": "一件用来演示字段形状的武器条目。",
          "fixed_affixes": [
            "example_affix_a"
          ],
          "series_set": "示例套"
        },
        {
          "name": "示例护腿",
          "slot": "legs",
          "quality": "white",
          "lv": 3,
          "series": "示例系列",
          "source": "示例来源"
        }
      ]
    },
    "equip_table": {
      "type": "object",
      "title": "EQUIP_ROSTER 整表：{装备 id: 装备}（条目本体见 $defs/equip）",
      "minProperties": 1,
      "propertyNames": {
        "pattern": "^eq_[a-z0-9_]+$",
        "description": "键 = 装备 id（编辑器列表显示的就是它；掉落池 / 副本掉落 / 物品图纸都按它引用）。"
      },
      "additionalProperties": {
        "type": "object"
      }
    },
    "series_sets": {
      "type": "object",
      "title": "SERIES_SETS（系列 → 套装名）源表：{系列名: 套装名}（导出期按 series 连接进条目 series_set）",
      "x-widget": "kv",
      "description": "源表：`键 = 系列名（显示名）`、`值 = 套装名`。它由**导出期按 series 连接**进条目的 series_set；查不到映射的系列不写那个字段（缺省 ≠ 空串）。",
      "minProperties": 1,
      "propertyNames": {
        "type": "string",
        "minLength": 1,
        "description": "键 = 系列名（与条目 `series` 逐字相同才算连得上 —— 改这里等于改连接键）。"
      },
      "additionalProperties": {
        "type": "string",
        "minLength": 1
      },
      "examples": [
        {
          "示例系列": "示例套"
        }
      ]
    },
    "series_fixed_affix": {
      "type": "object",
      "title": "SERIES_FIXED_AFFIX（装备名 → 固定词条 id 列表）源表：{装备名: [词条 id...]}（导出期按 name 连接进条目 fixed_affixes）",
      "x-widget": "kv",
      "description": "源表：`键 = 装备名`、`值 = 固定词条 id 列表（一项一个 id）`。导出期按 `name` 把它连接进条目 `fixed_affixes`。",
      "minProperties": 1,
      "propertyNames": {
        "type": "string",
        "minLength": 1,
        "description": "键 = 装备名（名册的 `name` 显示名；同名两件时导出器直接报错，不静默选一条）。"
      },
      "additionalProperties": {
        "title": "固定词条 id 列表",
        "type": "array",
        "description": "该装备的固定词条 id 表，指向词条域的表键。**空列表是合法且有意义的**（= 显式声明「这件装备没有固定词条」，与「源表里没有这个名字」是两回事）→ 故意不给 minItems。",
        "items": {
          "type": "string",
          "minLength": 1
        }
      },
      "examples": [
        {
          "示例短剑": [
            "example_affix_a"
          ]
        }
      ]
    }
  },
  "x-primary": "equip"
}

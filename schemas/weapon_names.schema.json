{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://saintessengine.local/schema/weapon_names.schema.json",
  "title": "武器类型名（weapon_names）—— 一条 = 一个武器类型 id → 中文名",
  "description": "原 `_eq_random_desc` 内联字面量（9 键 / 4 行），D8 进包内域。读口 = `content/drops.py:65-68` `records_from_domain(_PKG_ROOT, \"weapon_names\", order=(9 键真源插入序))` `.into(lambda _e: _e.get(\"name\"))`；消费 = 随机装备名生成（掉落 / 商店货架）。实测 9 键，与 `content/catalog_rules.WEAPON_TYPES` 同键集（sword/axe/mace/fist/spear/shield/bow/dagger/staff）。",
  "x-primary": "weapon_name_entry",
  "type": "object",
  "propertyNames": {
    "pattern": "^[a-z][a-z0-9_]*$",
    "description": "键 = 武器类型 id（实测 9 个，与 `catalog_rules.WEAPON_TYPES` 同集）"
  },
  "additionalProperties": {
    "$ref": "#/$defs/weapon_name_entry"
  },
  "$defs": {
    "weapon_name_entry": {
      "type": "object",
      "title": "一个武器类型",
      "description": "`{name}` 单字段（读口只取 `name`）。",
      "required": [
        "name"
      ],
      "additionalProperties": true,
      "properties": {
        "name": {
          "type": "string",
          "title": "中文武器名",
          "description": "实测 9/9 非空（战斧 / 长弓 / 短刃 …）。",
          "minLength": 1
        }
      }
    }
  }
}

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://saintessengine.local/schema/enemy_debuff_keys.schema.json",
  "title": "敌方减益键清单（enemy_debuff_keys）—— 一段 = {keys: [敌方减益键]}",
  "description": "敌方减益类的键清单（D9 进包内域；原模块级字面量元组，序 = 表内序）。读口 = content/mech/cond_procs.py:62-68（_DEBUFF_TBL.get(enemy_debuff_keys)）—— fail-closed：域缺 / 空 / 键型不符 → 点名抛。实测 7 条（def_down / spd_down / mon_atk_down / atk_down / stun / freeze / silence）。",
  "x-primary": "enemy_debuff_keys_group",
  "type": "object",
  "propertyNames": {
    "pattern": "^[A-Za-z_][A-Za-z0-9_]*$",
    "description": "键 = 源段名（实测 1 键：enemy_debuff_keys）"
  },
  "additionalProperties": {
    "$ref": "#/$defs/enemy_debuff_keys_group"
  },
  "$defs": {
    "enemy_debuff_keys_group": {
      "type": "object",
      "additionalProperties": true,
      "minProperties": 1,
      "title": "敌方减益键清单",
      "description": "只有 keys 一个字段。",
      "required": [
        "keys"
      ],
      "properties": {
        "keys": {
          "type": "array",
          "title": "敌方减益键",
          "description": "实测 7 条；序 = 表内序。",
          "minItems": 1,
          "items": {
            "type": "string",
            "title": "敌方减益键",
            "minLength": 1,
            "pattern": "^[a-z][a-z0-9_]*$"
          }
        }
      }
    }
  }
}
